import Route from '@ember/routing/route';

export default class ContactRoute extends Route {
  model() {
    // You can return data here that will be available in the template
    // For now, we'll just return an empty object
    return {};
  }
}
